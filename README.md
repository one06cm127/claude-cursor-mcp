# claude-cursor-mcp
A local MCP bridge that lets Claude.ai read and write files in your Cursor editor. Install the VSIX extension + run the Python server and Claude can see your open files, edit code, and more.
